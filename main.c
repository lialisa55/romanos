#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n;
    scanf("%d", &n);

    for(int j = 0; j < n; j++){
        int a, m = 0, d = 0, c = 0, l = 0, x = 0, v = 0, i = 0;
        scanf("%d", &a);
        while (a >= 1000){
            m++;
            a -= 1000;
        }
    }
    return 0;
}
